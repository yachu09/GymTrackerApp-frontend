import React from "react";
import { View, Text, StyleSheet } from "react-native";

const TestTabScreen = () => {
  return (
    <View>
      <Text>This is the TEST tab</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default TestTabScreen;
